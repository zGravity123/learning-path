Program Pzim ;
	v:array[1..3, 1..3] of integer
	i,j: integer;
	
procedure tabela
 Begin
 	randomize;
 	textcolor(cyan);
 	
 	for i := 1 to 4 do
 		Begin
 			for i := 1 to 4 do
 			 Begin
 			 		v[j,i] := random(50);
Begin
  
End.